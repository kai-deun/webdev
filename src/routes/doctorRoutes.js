const express = require('express');
const router = express.Router();

//router paths here
router.get('/set-prescription', (req, res)=> {
    
});

router.get('/delete-prescription', (req, res) => {

});

module.exports = router;